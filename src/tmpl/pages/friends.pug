extends ../include/layout/default.pug

block content
	include ../include/elem/user/mixins.pug

	.users-all
		.container-fluid
			ul.list-unstyled.users-list.row.mt-4
				- for (let i = 0; i < 5; ++i) {
					li.col-xxxl-3.col-xxl-4.col-md-6.mb-3
						.h-100
							+userCard(true)
				- }

	+inviteModal()
